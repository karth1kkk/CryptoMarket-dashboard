# frozen_string_literal: true
# Heroku and other hosts build from the monorepo root; the Ruby buildpack only looks
# for Gemfile/Gemfile.lock here. The real list of gems is kept in:
eval_gemfile "crypto-backend/Gemfile"
